#  ½ (b × h)

base = float(input("Enter the base of triangle: "))
height = float(input("Enter the height of triangle: "))

area_of_triangle = (1 / 2) * (base * height)

print("Area of triangle with base and height is: ", area_of_triangle)